# 造价核价助手 Skill

> 专为山川大人的徐州光伏零星工程核价工作而建，2026-04-14 创建

## 简介

本 skill 辅助完成徐州光伏零星工程维修审批单的核价工作，核心功能：
- 从维修审批单提取维修项（支持 OCR 识别扫描件）
- 从中标清单v2自动匹配综合单价
- 生成规范的 Excel 核价明细表
- 自动识别工程量与照片证据不符的情况

## 触发词

- "帮我核价"
- "套个价"
- "这个多少钱"
- "零星工程"
- "维修审批"
- "测算一下"
- "价格复核"

## 核心数据依据

### 中标清单（当前默认版本）

- **文件**：`D:\Document\xwechat_files\xiaozhaoxin2012_0054\msg\file\2026-03\徐州光伏2026年度零星工程施工招标-中标清单v2(2).xlsx`
- **版本**：v2，2026-03-24 起生效
- **有效期**：2026年12月31日
- **合同规则**：单价合同，下浮10%已含在中标单价，**结算不乘0.9**
- **含税税率**：9%

### 常用单价速查

| 项目 | 单价 | 子目位置 |
|------|------|---------|
| 卫生间管道疏通 | 97.48元/次 | 土建及修缮 |
| 小便感应器（感应延迟阀）| 500元/组 | 电气配件安装及拆除 |
| 小便延迟阀（普通机械式）| 134元/个 | 土建及修缮 |
| T8日光灯管 | 30元/根 | 灯具安装及拆除 |
| 木门锁拆换 | 64.62元/套 | 土建及修缮 |
| 木门锁锁芯拆换 | 36.41元/个 | 土建及修缮 |
| 合页拆换（不锈钢）| 36.41元/个 | 土建及修缮 |
| 平板灯（吸顶式）| 40元/个 | 灯具安装及拆除 |
| 内墙面砖铺贴（人工）| 168元/m² | 人工报价及费率 |
| 技工 | 420元/工日 | 人工报价及费率 |
| 普工 | 280元/工日 | 人工报价及费率 |
| 拆除台盆/拖把池 | 34.02元/套 | 土建及修缮 |

## 标准工作流

### Step 1：读取审批单

```python
import openpyxl, zipfile, os

def read_approval_form(path):
    """读取维修审批单，提取所有维修项"""
    wb = openpyxl.load_workbook(path, data_only=True)
    ws = wb.active
    items = []
    for row in range(5, ws.max_row + 1):
        desc = ws.cell(row=row, column=2).value  # 维修内容
        qty  = ws.cell(row=row, column=3).value  # 工程量
        area = ws.cell(row=row, column=4).value  # 故障区域
        if desc:
            items.append({"row": row, "desc": desc, "qty": qty, "area": area})
    return items

def extract_photos(path, out_dir="审批单图片"):
    """从Excel中提取嵌入图片"""
    os.makedirs(out_dir, exist_ok=True)
    with zipfile.ZipFile(path, 'r') as z:
        for img in z.namelist():
            if img.startswith('xl/media/'):
                z.extract(img, out_dir)
```

### Step 2：匹配清单子目

- 关键词精确匹配优先
- "感应"、"红外" → 小便感应器（500元）
- "阀"、"延时" → 小便延迟阀（134元）
- 区分不开时用"小便"+"感应"两个关键词交叉搜索
- 无精确匹配 → 估价，标记 ⚠️

### Step 3：生成核价表

输出 Excel，含以下列：
`序号 | 报修业务 | 故障区域 | 维修内容（原文）| 清单子目 | 单位 | 工程量 | 综合单价(元) | 合价(元)`

样式规范：
- 表头蓝色（4472C4）
- ⚠️ 估价项黄色底
- ✅ 更正项黄色底+说明
- 合计行+含税合计行（9%）
- 说明区列明依据

### Step 4：输出路径

`C:\Users\山川\Desktop\龙虾同步文件夹\核价明细_{来源文件名}.xlsx`

## 照片核对规范

**必须检查照片的情况：**
1. "感应"类 → 确认是感应还是普通（差价3倍+）
2. "破损"、"损坏" → 确认损坏程度，判断修复还是更换
3. 防水/瓷砖 → 照片估算实际工程量
4. 门/柜 → 确认材质（铁皮/木/铝合金）

## 注意事项

- 永远用 `data_only=True` 读取 Excel，避免读到公式字符串
- 中文路径用 Python 脚本处理，不用 PowerShell
- 审批单中"补单子"表示漏报的维修项，依然有效
- 多个地点同一类维修 → 分列计列（如两处疏通分开计）
- 税率 9%，合同规定下浮10%已含在单价里

## 经验教训

1. ⚠️ **感应阀 vs 普通阀**：外观相似但单价差4倍，必须核对照片或文字说明
2. ⚠️ **照片优先**：审批单文字描述与实际可能有出入，照片是核验依据
3. ⚠️ **工程量实测**：瓷砖/抹灰等按经验值暂估后，建议现场实测调整
4. ⚠️ **完整列项**：不能合并不同地点的同类维修项
